#!/usr/bin/env python3
"""Operator entry point for the scorecard data-sufficiency audit
(backend: core/llm/scorecard/audit.py). Phase 1a of the
calibrated-aggregation arc — gates the Beta-prior parameterisation in
Phase 1b.

Operator-typed diagnostic, co-located with the subsystem it audits
(matching core/inventory/scripts/binary-oracle-closed-loop). Run with
--help for usage.
"""
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parents[4]))

from core.llm.scorecard.audit import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
